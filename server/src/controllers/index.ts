import { register, login, logout, refreshAccessToken, updateAvatar, updateUser } from "./user.controller";
export {
    register,
    login,
    logout,
    refreshAccessToken,
    updateAvatar,
    updateUser
}